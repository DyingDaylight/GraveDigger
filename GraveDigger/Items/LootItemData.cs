namespace GraveDigger.Items;

public class LootItemData : ItemData
{
    public LootItemData(string id, string name, string description, string spriteName, int price, int maxStackSize) 
        : base(id, name, description, spriteName, price, maxStackSize)
    {
        
    }
}